SET(ZMQ_INCLUDE_DIR zmq)
INSTALL(DIRECTORY ${ZMQ_INCLUDE_DIR} DESTINATION include)

if(ARM)
    INSTALL(FILES ppmac/libmath/libmath.so DESTINATION lib)
    INSTALL(FILES ppmac/libopener/libopener.a DESTINATION lib)
    INSTALL(FILES ppmac/libppmac/libppmac.so DESTINATION lib)
    INSTALL(FILES xenomai/libpthread_rt.so.1 DESTINATION lib)
    INSTALL(FILES xenomai/libxenomai.so.0 DESTINATION lib)
endif()
